# build output
dist/
# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables — NEVER commit credentials
.env
.env.*
!.env.example

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/

# ── secret-hardening 2026-06-20 ───────────────────────────────────────────
# Local helper scripts have historically carried inline tokens. Never commit them.
*.command
*_result.txt
# Local Claude settings have carried dev secrets (e.g. devsecret123). Never commit.
.claude/settings.local.json

# local archive of retired scripts / content (not for git)
_archive/
# wrangler local state
.wrangler/
workers/**/.wrangler/
# locally-installed claude skills
.claude/skills/
